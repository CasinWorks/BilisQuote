# Optional: override demo login (still exposed in client bundle — use only as a simple gate).
# In Vercel: Project Settings → Environment Variables → add these for Production.
VITE_AUTH_USERNAME=admin
VITE_AUTH_PASSWORD=admin
